        class foo(int i, int j) {
            bar() {
                def k = @i + @j
                def b = 2
                return b
            }
        }

        def f = foo(2, 5)

        def c = f.bar()

        PRINT(TO_STRING(c))
